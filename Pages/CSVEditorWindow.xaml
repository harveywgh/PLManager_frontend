<Window x:Class="PLManager.Windows.CSVEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="Éditeur CSV"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Window.Resources>
        <!-- 🎨 Style local pour boutons gris -->
        <Style x:Key="LocalGrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#B0B0B0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D5D5D5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#EEEEEE"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ✅ Grid qui englobe tout -->
    <Grid>

        <!-- 🔹 Contenu principal -->
        <DockPanel>

            <ScrollViewer DockPanel.Dock="Top"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled"
                          Margin="10,10,10,0">
                <StackPanel x:Name="FileButtonsPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            Height="50"
                            Margin="0,0,0,5" />
            </ScrollViewer>

            <!-- ✅ Notification toast alignée à droite -->
            <StackPanel DockPanel.Dock="Top"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,0,20,5">
                <Border x:Name="ToastContainer"
            Background="White"
            BorderBrush="Black"
            BorderThickness="1"
            CornerRadius="6"
            Padding="4,2"
            Visibility="Collapsed"
            Opacity="0.95">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="✔"
                       FontWeight="Bold"
                       Margin="0,0,6,0"
                       Foreground="Black"
                       FontSize="12"/>
                        <TextBlock x:Name="ToastNotification"
                       Text="Fichier enregistré !"
                       Foreground="Black"
                       FontSize="11"
                       VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,5,0,5">
                <Button Content="Enregistrer"
                        Width="100"
                        Height="30"
                        Click="SaveToCurrentFile_Click"
                        Style="{StaticResource LocalGrayButtonStyle}" />
                <Button Content="Enregistrer sous"
                        Width="130"
                        Height="30"
                        Click="SaveAsCsv_Click"
                        Style="{StaticResource LocalGrayButtonStyle}" />
            </StackPanel>
            
           

            <wfi:WindowsFormsHost x:Name="GridHost"
                                  Margin="10"
                                  VerticalAlignment="Stretch"
                                  HorizontalAlignment="Stretch"/>
        </DockPanel>
    </Grid>
</Window>
